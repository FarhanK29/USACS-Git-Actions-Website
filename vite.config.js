import { defineConfig } from 'vite';

export default defineConfig({
  // If your URL is username.github.io/my-repo/, base should be '/my-repo/'
  // Using './' is a safe 'catch-all' for simple projects
  base: './', 
});